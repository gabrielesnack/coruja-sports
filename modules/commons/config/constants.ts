import { ContainerProps } from '@chakra-ui/react'

export const HEADER_SIZE = '70px'
export const LOGO_SIZE = `calc(${HEADER_SIZE} * 0.75)`
export const CONTAINER_PROPS: ContainerProps = {
  maxWidth: 'container.xl',
  width: '100%',
  px: ['1.5rem', null, null, null, '1rem'],
}
